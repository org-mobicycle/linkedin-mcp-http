{
  "name": "linkedin-mcp-http",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP MCP server for LinkedIn posting and management (Cloudflare Workers)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.59.2"
  },
  "dependencies": {
    "agents": "0.3.10",
    "zod": "^3.24.1"
  }
}
